<Window x:Class="FrontendForMame.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontendForMame.UI"
        mc:Ignorable="d"
        Title="Frontend for MAME" Height="450" Width="800" Background="#FF150F44" WindowStyle="None" WindowState="Maximized"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" FontSize="48" FontStyle="Italic" FontWeight="Bold" Foreground="#FF94BECC" Margin="20,0,0,0">
                Frontend for MAME
            </Label>
            <Label Grid.Column="1" FontSize="16" Foreground="#FF94BECC" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Path=Version, StringFormat='v{0}'}" />
            </Label>
            <Button Grid.Column="2" BorderBrush="#00000000" Background="#00000000" Click="Exit_Click">
                <Image Source="/Data/Exit.png"></Image>
            </Button>
            <Button Grid.Column="3" BorderBrush="#00000000" Background="#00000000" Click="Shutdown_Click">
                <Image Source="/Data/Shutdown.png"></Image>
            </Button>
        </Grid>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" FontSize="16" Foreground="#FF94BECC" HorizontalAlignment="Center" VerticalAlignment="Center">
                Controller 1
            </Label>
            <Label Grid.Column="1" FontSize="16" Foreground="#FF94BECC" HorizontalAlignment="Center" VerticalAlignment="Center">
                Controller 2
            </Label>
        </Grid>
        <Grid></Grid>
    </DockPanel>
</Window>
