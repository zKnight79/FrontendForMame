<Window x:Class="FrontendForMame.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontendForMame.UI"
        mc:Ignorable="d"
        Title="Frontend for MAME" Height="450" Width="800" Background="#FF150F44" WindowStyle="None" WindowState="Maximized"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Style.Setters>
                <Setter Property="BorderBrush" Value="#00000000" />
                <Setter Property="Background" Value="#00000000" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Label">
            <Style.Setters>
                <Setter Property="Foreground" Value="#FF94BECC" />
                <Setter Property="FontSize" Value="20" />
            </Style.Setters>
        </Style>
        <BitmapImage x:Key="DefaultLogo" UriSource="/Data/Missing.png" />
        <BitmapImage x:Key="DefaultPreview" UriSource="/Data/NoPreview.png" />
    </Window.Resources>
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" FontSize="48" FontStyle="Italic" FontWeight="Bold" Margin="20,0,0,0">
                <TextBlock Text="{Binding FrontendTitle, FallbackValue='Frontend for MAME', TargetNullValue='Frontend for MAME'}" />
            </Label>
            <Label Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Version, StringFormat='v{0}', FallbackValue='Version'}" />
            </Label>
            <Button Grid.Column="2" Click="Exit_Click">
                <Image Source="/Data/Exit.png"></Image>
            </Button>
            <Button Grid.Column="3" Click="Shutdown_Click">
                <Image Source="/Data/Shutdown.png"></Image>
            </Button>
        </Grid>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Controller1Name, FallbackValue='Controller 1', TargetNullValue='Controller 1'}" />
            </Label>
            <Label Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Controller2Name, FallbackValue='Controller 2', TargetNullValue='Controller 2'}" />
            </Label>
        </Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="160" />
                <RowDefinition Height="*" />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Source="{Binding GameLogoPath, FallbackValue={StaticResource DefaultLogo}, TargetNullValue={StaticResource DefaultLogo}}" />
            <MediaElement Grid.Row="1" Visibility="Hidden" x:Name="GameSnapControl" Source="{Binding GameSnapPath}" LoadedBehavior="Manual" />
            <Image Grid.Row="1" x:Name="GamePreviewControl" Source="{Binding GamePreviewPath, FallbackValue={StaticResource DefaultPreview}, TargetNullValue={StaticResource DefaultPreview}}" />
            <Label Grid.Row="2" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="{Binding GameFullName, FallbackValue='Game Full Name', TargetNullValue='Game Full Name'}" />
            </Label>
        </Grid>
    </DockPanel>
</Window>
